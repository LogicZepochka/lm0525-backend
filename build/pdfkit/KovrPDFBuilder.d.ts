import { PrintData } from "./types/types";
declare const _default: {
    generatePDF(remain: PrintData, PDF: PDFKit.PDFDocument, barPng: any, qr: Buffer<ArrayBufferLike>): Promise<void>;
};
export default _default;
